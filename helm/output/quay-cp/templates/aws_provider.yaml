---
# Source: quay-cp/templates/aws_provider.yaml
apiVersion: aws.crossplane.io/v1alpha3
kind: Provider
metadata:
  name: aws-provider
spec:
  region: "us-east-2"
  credentialsSecretRef:
    namespace: crossplane-system
    name: aws-creds
    key: key
