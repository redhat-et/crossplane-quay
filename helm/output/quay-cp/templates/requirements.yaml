---
# Source: quay-cp/templates/requirements.yaml
apiVersion: storage.example.org/v1alpha1
kind: BucketRequirement
metadata:
  name: s3buckettestquayredhat
  namespace: default
spec:
  parameters:
    name: s3buckettestquayredhat
  compositionSelector:
    matchLabels:
      provider: aws
  writeConnectionSecretToRef:
    name: bucket-conn
    namespace: default
---
# Source: quay-cp/templates/requirements.yaml
apiVersion: database.example.org/v1alpha1
kind: PostgreSQLInstanceRequirement
metadata:
  name: my-db
  namespace: default
spec:
  parameters:
    storageGB: 10
  compositionSelector:
    matchLabels:
      provider: aws
  writeConnectionSecretToRef:
    name: db-conn
    namespace: default
---
# Source: quay-cp/templates/requirements.yaml
apiVersion: cache.example.org/v1alpha1
kind: RedisClusterRequirement
metadata:
  name: my-redis
  namespace: default
spec:
  compositionSelector:
    matchLabels:
      provider: aws
  writeConnectionSecretToRef:
    name: redis-conn
    namespace: default
